COLUMN TABLE "CDS_DS.T_EMP_DEF_ACC" (
  PERSONNEL_ID NVARCHAR(32) NOT NULL,
  ORDER_NUM NVARCHAR(64) NOT NULL,
  BAND_LEVEL NVARCHAR(100),
  JOB_DESC NVARCHAR(100),
  ORDER_DESC NVARCHAR(100),
  PRIMARY KEY(PERSONNEL_ID, ORDER_NUM)
)