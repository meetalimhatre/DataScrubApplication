COLUMN TABLE "CDS_DS.T_EMP_ORDER" (
  PERNR NVARCHAR(8) NOT NULL,
  BEGDA DATE NOT NULL,
  ENDDA DATE NOT NULL,
  AUFNR NVARCHAR(12) NOT NULL,
  KTEXT NVARCHAR(40),
  VORNR NVARCHAR(4),
  AEDTM DATE,
  PROZT DECIMAL(5, 2),
  PRIMARY KEY(PERNR, BEGDA, ENDDA, AUFNR)
)