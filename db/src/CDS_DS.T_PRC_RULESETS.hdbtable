COLUMN TABLE "CDS_DS.T_PRC_RULESETS" (
  PRC_ID BIGINT NOT NULL,
  RULESET_NAME NVARCHAR(30) NOT NULL,
  RULE_NAME NVARCHAR(30) NOT NULL,
  RULESET_DESCRIPTION NVARCHAR(200),
  PRIMARY KEY(PRC_ID, RULESET_NAME, RULE_NAME)
)